import React from 'react';
import useAuth from '../../../Hooks/useAuth';

const DashboardInfo = () => {
    const {user} = useAuth();
    return (
        <div>
            
        </div>
    );
};

export default DashboardInfo;